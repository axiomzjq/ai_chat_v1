# 近期功能更新日志

> 记录 2026-06-25 至 2026-07-05 期间的所有功能变更
> 最后更新：2026-07-09

---

## 1. 提示词架构重构（2026-07-05）

### 变更内容

将原来的"一份大提示词"改为"基础提示词 + 参考文件"分离架构：

```
src/lib/prompts.ts          ← 6 个基础提示词（角色定义 + 职责说明）
refs/                        ← 7 个参考 .txt 文件（详细方法论）
public/refs/                 ← 浏览器可访问的副本（fetch 加载）
```

### 基础提示词（prompts.ts）

| Prompt | 用途 | 输出格式 |
|--------|------|---------|
| `INTERVIEW_SYSTEM_PROMPT` | 访谈对话 | 自然语言 |
| `INFO_SYSTEM_PROMPT` | 企业/信息分析 | 自然语言 |
| `POSITIONING_SYSTEM_PROMPT` | 定位对话+报告 | 自然语言 |
| `COPYWRITING_SYSTEM_PROMPT` | 文案思路整理 | 自然语言（对话） |
| `COPYWRITING_GENERATE_SYSTEM_PROMPT` | 文案脚本生成 | 强制 JSON |
| `TOPIC_SYSTEM_PROMPT` | 选题生成 | 强制 JSON |

### 参考文件（refs/）

| 步骤 | 参考文件 | 说明 |
|------|---------|------|
| 访谈 | `refs/interview/客户访谈参考手册.txt` | 访谈方法论 |
| 定位 | `refs/positioning/客户定位访谈参考手册.txt` | 定位判断方法 |
| 定位 | `refs/positioning/写作技巧提示词_精华版.txt` | 写作技巧（与选题共用） |
| 选题 | `refs/topic/选题提示词_基于访谈结果.txt` | 选题方法论 |
| 选题 | `refs/topic/写作技巧提示词_精华版.txt` | 写作技巧（与定位共用） |
| 文案 | `refs/copywriting/客户采访与选题提示词.txt` | 采访框架 |
| 文案 | `refs/copywriting/文案审核提示词.txt` | 审核标准 |

### 上下文注入方式

```typescript
// 1. 加载参考文件（从 public/refs/ fetch，带缓存）
const refContent = await loadStepRefs('interview');

// 2. 注入到 AI 请求
system: BASE_PROMPT + (refContent ? `

【参考文件 · xxx】：
${refContent}` : '')
```

### 关键特性

- **参考文件缓存**：`refsCache` 只加载一次，后续直接读取缓存
- **按步骤加载**：每个步骤只加载自己需要的参考文件
- **基础提示词精简**：只定义角色和职责，详细方法论从 txt 文件加载

### 相关文档

- `knowledge-base/提示词文件架构.md` — 完整文件架构
- `knowledge-base/提示词架构说明.md` — 调用方式说明

---

## 2. 多用户数据隔离修复（2026-07-05）

### 问题

同一台浏览器上，不同账号登录会共享 localStorage 数据，导致管理员测试的数据被普通用户看到。

### 根因

所有 localStorage key 都是全局的，没有按用户 ID 做区分：

```typescript
// ❌ 修复前 - 共享
localStorage.getItem('founder_ip_history')
localStorage.getItem('result_interview_report')

// ✅ 修复后 - 按用户隔离
localStorage.getItem(`founder_ip_history_${userId}`)
localStorage.getItem(`result_interview_report_${userId}`)
```

### 修复方案

1. **新增 `getUserScopedKey()`** — 生成用户隔离的 key
2. **`saveResultToStorage` / `loadResultFromStorage`** — 新增 `userId` 参数
3. **`loadUIState()`** — 新增 `userId` 参数
4. **`loadUserLocalData()`** — 登录后加载该用户的所有本地数据
5. **`handleLogout`** — 登出时清除该用户的全部 localStorage 数据

### 隔离的 localStorage key

| 修复前（共享） | 修复后（按用户隔离） |
|--------------|-------------------|
| `founder_ip_history` | `founder_ip_history_{userId}` |
| `result_interview_report` | `result_interview_report_{userId}` |
| `result_positioning_report` | `result_positioning_report_{userId}` |
| `result_info_report` | `result_info_report_{userId}` |
| `result_topic_pool` | `result_topic_pool_{userId}` |
| `ai_chat_ui_state` | `ai_chat_ui_state_{userId}` |

### 数据存储说明

| 存储位置 | 数据 | 按用户隔离 | 状态 |
|---------|------|-----------|------|
| **后端数据库** | 对话消息、访谈结果、定位报告、选题池、文案 | ✅ 按 `user_id` 隔离 | 正常 |
| **前端 localStorage** | 同上 + UI 状态 | ✅ 按 `userId` 隔离 | **已修复** |

---

## 3. 步骤解锁逻辑修复（2026-07-05）

### 问题

`isStepUnlocked` 调用时传入了空数组 `[]` 而非 `state.topicPool`，导致文案步骤的锁检查 `!!topicPool.length` 永远为 `false`。

### 影响

用户生成选题池后，导航栏和侧边栏的"文案"标签一直显示为锁定状态，与实际情况不一致。

### 修复

所有 4 处 `isStepUnlocked` 调用都改为传入 `state.topicPool`：

| 位置 | 修复前 | 修复后 |
|------|--------|--------|
| 主导航（行 334） | `[]` | `state.topicPool` |
| 访谈页"下一步"按钮（行 2325） | `[]` | `state.topicPool` |
| 访谈页"下一步"按钮样式（行 2333） | `[]` | `state.topicPool` |
| 侧边导航（行 4217） | `[]` | `state.topicPool` |

---

## 4. 选题页面空状态提示（2026-07-05）

### 变更

选题页面不再默认显示 Demo 数据，而是根据状态显示不同内容：

| 状态 | 条件 | 显示内容 |
|------|------|---------|
| **空状态** | `topicPool` 为空 + `idle` | 图标 + "尚未生成选题池" + 生成按钮 |
| **加载中** | `generating` | 旋转动画 + "AI 正在生成..." |
| **有数据** | `completed` | 真实 AI 生成的选题内容 |
| **降级** | `demo_fallback` | Demo 数据 + "重新生成"按钮 |

### 关键修改

- AI 降级数据（解析失败/网络错误）不再保存到 localStorage
- 刷新后无数据时显示空状态，不会自动加载 Demo
- `loadUserLocalData` 登录后正确设置 `topicGenerationStatus`

---

## 5. 定位方案 3 版独立输出（2026-07-05）

### 问题

AI 只生成一版定位方案，没有按要求输出 3 个独立方案。

### 修复

1. **用户 prompt 增加明确的格式要求**：
   ```
   ### 定位方案 1：[方案名称]
   [完整方案内容]

   ### 定位方案 2：[方案名称]
   [完整方案内容]

   ### 定位方案 3：[方案名称]
   [完整方案内容]
   ```

2. **分割正则**从 `/(?=###\s+定位分析|###\s+定位方案)/` 改为 `/(?=###\s+定位方案\s*\d)/`，精确匹配带编号的方案标题。

---

## 6. 调试模式增强（2026-07-05）

### 新增按钮

在调试模式（`DEBUG_MODE !== 'off'`）下，页面右上角新增 **"清除本地缓存"** 按钮：

| 按钮 | 图标 | 功能 |
|------|------|------|
| 导出调试日志 | Bug | 导出 console 日志 |
| 一键还原 | Trash2 | 清空当前会话所有数据 |
| **清除本地缓存** | Database | 清除当前用户的 localStorage |

### 清除本地缓存功能

- 清除该用户的所有 localStorage 数据
- 清除 auth token 缓存
- **不影响后端数据库**
- 清除后自动刷新页面

### 当前调试模式

```typescript
// src/lib/debug.ts
export const DEBUG_MODE: 'dev' | 'internal' | 'off' = 'internal';
```

---

## 7. 管理员/普通用户逻辑锁（2026-06-25）

### 管理员逻辑锁

| 步骤 | 解锁条件 | 实际效果 |
|------|---------|---------|
| 访谈 | 始终解锁 | ✅ 随时可进 |
| 定位 | `userRole === 'admin'` 直接 `return true` | ✅ 无需任何前置条件 |
| 选题 | 同上 | ✅ 无需任何前置条件 |
| 文案 | 同上 | ✅ 无需任何前置条件 |
| 历史 | 始终解锁 | ✅ 随时可进 |

### 普通用户逻辑锁

| 步骤 | 解锁条件 | 依赖的前序结果 |
|------|---------|--------------|
| 访谈 | 始终解锁 | 无 |
| 定位 | `!!interviewReport` | 必须完成访谈并生成报告 |
| 选题 | `!!interviewReport` | 必须完成访谈并生成报告 |
| 文案 | `!!topicPool.length` | 必须生成选题池 |
| 历史 | 始终解锁 | 无 |

---

## 8. 选题页面 UI 优化（2026-06-28）

### 变更内容

1. **文件夹样式标签页**：阶段标签使用 `rounded-t-lg` + `-mb-px` 与简介面板连接
2. **字体增大**：简介标题 `text-base`，正文 `text-sm`
3. **选题卡片压缩**：水平布局，隐藏次要字段
4. **按钮样式**：黑色实心 `bg-black text-white rounded-lg/xl`

### 相关文档

- `knowledge-base/选题页面UI规范.md` — 完整 UI 规范

---

## 9. 五步工作流（2026-06-25）

### 变更

从四步工作流（访谈→定位→文案→历史）改为五步工作流（访谈→定位→选题→文案→历史）。

### 步骤解锁逻辑

| 步骤 | Agent | 解锁条件 |
|------|-------|---------|
| **访谈** | 访谈顾问 | 始终可进 |
| **定位** | 定位顾问 | 完成访谈并生成报告 |
| **选题** | 选题顾问 | 完成访谈 |
| **文案** | 文案顾问 | 完成访谈并生成选题池 |
| **历史** | — | 始终可进 |

---

## 10. 文案提示词拆分（2026-06-25）

### 变更

文案页面拆分为两个独立 Prompt：

| Prompt | 用途 | 输出格式 |
|--------|------|---------|
| `COPYWRITING_SYSTEM_PROMPT` | 思路整理对话 | 自然语言 |
| `COPYWRITING_GENERATE_SYSTEM_PROMPT` | 脚本生成 | 强制 JSON |

---

## 11. 移除文案顾问对话模式（2026-07-09）

### 变更内容

移除文案页面的"文案顾问"对话模式（思路整理），选中选题后直接生成文案。

**移除的功能：**
- 文案页面的对话输入框和聊天界面
- `handleCopywritingMessage` 函数（AI 思路整理对话）
- `analyzeCopywritingInteraction` 函数（对话分析）
- `isCopywritingChatMode` / `copywritingMessages` 状态
- `copywritingTopic` 输入状态
- `selectedTopicAutoGenRef` 防重复触发逻辑

**新的文案页流程：**

1. 用户在选题页面点击某张选题卡片
2. 自动调用 `generateCopywriting(topic)`，传入完整选题信息（标题、爆款类型、3秒钩子、平台等）
3. 跳转到文案页面，显示加载状态
4. AI 直接输出 3 个标题 + 1 篇口播文案（max_tokens: 16000）
5. 用户可复制文案、保存到历史、或重新选择选题

**代码简化：**
- 文案页渲染从 ~230 行缩减到 ~100 行
- 移除了 `chatContext` 构建逻辑（不再有对话上下文）
- `generateCopywriting` prompt 不再包含对话历史
- `HistoryItem` 移除 `copywritingMessages` 和 `isCopywritingChatMode` 字段

### 相关文件

| 文件 | 变更 |
|------|------|
| `src/App.tsx` | 移除对话模式所有相关代码，简化文案页面 |
| `knowledge-base/近期功能更新日志.md` | 本文档 |

---

## 12. 通用 JSON 解析器提取（2026-07-09）

### 问题

`generateCopywriting` 使用裸 `JSON.parse(cwText)` 解析 AI 返回，当 AI 将 JSON 用 Markdown 代码块包裹（\`\`\`json ... \`\`\`）时，解析失败并报错：
```
JSON Parse error: Unrecognized token '`'
```

### 影响

文案生成每次失败，用户无法获得文案结果。

### 修复

提取通用 `parseJsonResponse(aiResponse)` 函数，实现 4 层解析策略：

| 策略 | 方法 | 适用场景 |
|------|------|---------|
| 1 | `JSON.parse` 直接解析 | AI 返回纯 JSON |
| 2 | 正则提取 \`\`\` 代码块内容 | AI 用 Markdown 包裹 JSON |
| 3 | 提取第一个 `{` 到最后一个 `}` | JSON 前后有废话/说明文字 |
| 3b | `repairTruncatedJson` 补全缺失括号 | AI 输出被截断 |

`parseTopicPool` 和 `generateCopywriting` 都使用此函数，避免重复代码。

### 相关文件

| 文件 | 变更 |
|------|------|
| `src/App.tsx` | 新增 `parseJsonResponse`，`parseTopicPool` 和 `generateCopywriting` 都调用它 |

---

## 13. localStorage 用户数据缓存（2026-07-09）

### 变更内容

登录后自动缓存该用户的关键数据到 localStorage，减少重复 API 请求。所有 localStorage key 通过 `getUserScopedKey(baseKey, userId)` 添加 `_{userId}` 后缀，实现用户隔离。

### 缓存的数据

| 数据类型 | localStorage Key | 后端存储 |
|---------|-----------------|---------|
| 访谈消息历史 | `founder_ip_history_{userId}` | `conversations` 表 |
| 访谈结果 | `result_interview_report_{userId}` | `user_profiles.interview_data` |
| 定位报告 | `result_positioning_report_{userId}` | `user_profiles.positioning_report` |
| 选题池 | `result_topic_pool_{userId}` | `user_profiles.topic_pool` |
| 文案输出 | `copywritingOutput`（state 内存） | `user_profiles.copywriting_data` |
| UI 状态 | `ai_chat_ui_state_{userId}` | 无（纯前端） |

### 关键函数

| 函数 | 功能 |
|------|------|
| `getUserScopedKey(baseKey, userId)` | 生成用户隔离的 localStorage key |
| `loadUserLocalData(userId)` | 登录后加载该用户的全部本地数据 |
| `handleLogout()` | 登出时清除该用户的全部 localStorage 数据 |

### 相关文件

| 文件 | 变更 |
|------|------|
| `src/App.tsx` | 新增 `loadUserLocalData`、`getUserScopedKey`，`handleLogout` 增加清除逻辑 |

---

## 14. parseJsonResponse 增强调试日志（2026-07-09）

### 变更内容

在 `parseJsonResponse` 和 `generateCopywriting` 中增加详细调试日志，方便快速定位 AI 返回格式问题。

### 新增日志

| 日志前缀 | 内容 |
|---------|------|
| `[parseJson]` | AI 返回内容长度 + 前 200 字符 |
| `[parseJson] 策略1/2/3` | 每层策略的尝试结果（成功/失败原因） |
| `[generateCopywriting]` | AI 完整返回（前 500 字符）+ 解析结果 + 分步状态 |

### 用途

当 AI 返回格式异常时，控制台日志会精确显示：
- AI 实际返回了什么格式
- 哪个解析策略成功了/失败了
- 如果全部失败，具体是什么内容导致解析失败

### 相关文件

| 文件 | 变更 |
|------|------|
| `src/App.tsx` | 增强 `parseJsonResponse` 和 `generateCopywriting` 的调试日志 |

---

*最后更新：2026-07-09*
